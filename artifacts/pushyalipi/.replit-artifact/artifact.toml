kind = "web"
previewPath = "/"
title = "Pushyalipi Kundli"
description = "A private Vedic astrology web app for creating complete birth charts and Kundli readings from birth details, with printable reports and visual chart insights."
version = "1.0.0"
id = "artifacts/pushyalipi"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21591

[services.development]
run = "pnpm --filter @workspace/pushyalipi run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pushyalipi", "run", "build" ]
serve = "static"
publicDir = "artifacts/pushyalipi/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21591"
BASE_PATH = "/"
